On the first model turn after this session starts, resumes, or is cleared, call acs_identity. If it reports an unbound state, call acs_register immediately; ACS assigns a generic session-derived slug, so do not ask the user for a name or claim code. After identity is bound, call acs_agents_list once before handling the user request to inspect currently active agents and their activity for useful coordination.
